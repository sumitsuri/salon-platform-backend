package com.salonplatform.domain.enums;

public enum PackagePlanType {
    /** Fixed services and quantities defined at plan creation. */
    SERVICE_BUNDLE,
    /** Prepaid credit redeemable against any branch services at billing time. */
    VALUE_CREDIT
}
