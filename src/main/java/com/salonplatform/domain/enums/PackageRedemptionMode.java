package com.salonplatform.domain.enums;

/** SINGLE_VISIT: all included services should be redeemed in one visit. MULTI_VISIT: partial across visits until expiry. */
public enum PackageRedemptionMode {
    SINGLE_VISIT,
    MULTI_VISIT
}
