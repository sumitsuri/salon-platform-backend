package com.salonplatform.domain.enums;

/** How a branch positions itself on Google — drives Local Spotlight keywords and rival discovery. */
public enum BranchBusinessType {
    SALON,
    SPA,
    SALON_AND_SPA
}
